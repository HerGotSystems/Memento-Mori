
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Memento Mori: Life Priority Calculator',
  description: 'A gentle statistical reminder to live meaningfully - combining mortality awareness with philosophical wisdom',
  keywords: 'memento mori, mortality, life priorities, philosophy, statistics, actuarial',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
